import { styled } from 'styled-components';

export const StyledAdminLayoutWrapper = styled.main`
	max-width: 1440px;
	margin: auto;
`;
export const StyledAdminLayoutMain = styled.section`
	display: flex;
	min-height: calc(100vh - 40px);
`;
export const StyledAdminLayoutContentWrapper = styled.section`
	width: calc(100% - 253px);
	padding: 16px;
`;
