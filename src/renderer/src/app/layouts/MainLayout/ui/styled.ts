import { styled } from 'styled-components';

export const StyledMainLayout = styled.main``;
export const StyledMainLayoutContent = styled.div`
	background: #161c24;
	padding: 8px;
`;
export const StyledMainLayoutContentInner = styled.div`
	min-height: calc(100vh - 56px);
	background: #919eab14;
	overflow: hidden;
	border-radius: 12px;
`;
